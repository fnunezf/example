    def button05Tabla010(self):
        self.table01.setRowCount(len(self.numbers_list))
        for row, number in enumerate(self.numbers_list):
            item = QTableWidgetItem(str(number))
            self.table01.setItem(row, 0, item)
